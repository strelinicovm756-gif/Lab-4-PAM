name: data
description: Data layer with models, datasources and repository implementations
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  domain:
    path: ../domain
  http: ^1.1.0
  json_annotation: ^4.9.0
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  test: ^1.25.0
  lints: ^6.0.0