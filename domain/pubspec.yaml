name: domain
description: Domain layer with business logic and entities
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
# Pure Dart - no external dependencies

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0